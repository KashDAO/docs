---
title: 'explain'
description: 'Look up one or more error codes and their recommended recovery steps.'
---

Look up one or more error codes and their recommended recovery steps.

> Generated from `kash docs --json`. Run `kash explain --help` for full usage and examples.

## Usage

```bash
kash explain [codes] [options]
```

**Arguments**

- `codes` (optional) (variadic) — error codes to explain (omit to list every known code)
