---
title: 'trace'
description: 'Fetch the curated event timeline for a trade\'s correlation id.'
---

Fetch the curated event timeline for a trade's correlation id.

> Generated from `kash docs --json`. Run `kash trace --help` for full usage and examples.

## Usage

```bash
kash trace <correlationId> [options]
```

**Arguments**

- `correlationId` — correlation UUID — see `kash trade status <id> --json | jq -r .correlationId`
