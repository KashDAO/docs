---
title: 'account'
description: 'Read-only account surfaces (usage telemetry, etc.).'
---

Read-only account surfaces (usage telemetry, etc.).

> Generated from `kash docs --json`. Each command's behaviour is documented in-binary via `kash account <subcommand> --help` — this page is the structured reference.

## Usage

```bash
kash account <subcommand> [options]
```

## Subcommands

### `kash account usage`

Show per-key telemetry summary (24h / 7d / 30d windows).
