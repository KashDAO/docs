---
title: 'health'
description: 'Check connectivity to the Kash API. Exits 1 when not ok. Honors --timeout-ms (default 5000).'
---

Check connectivity to the Kash API. Exits 1 when not ok. Honors --timeout-ms (default 5000).

> Generated from `kash docs --json`. Run `kash health --help` for full usage and examples.

## Usage

```bash
kash health [options]
```
