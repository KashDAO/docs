---
title: 'setup'
description: 'Interactive first-run wizard: configure an API key, verify, install completion.'
---

Interactive first-run wizard: configure an API key, verify, install completion.

> Generated from `kash docs --json`. Run `kash setup --help` for full usage and examples.

## Usage

```bash
kash setup [options]
```

## Options

| Flag | Description |
| --- | --- |
| `-y, --yes` | accept all defaults and skip optional prompts (non-interactive) |
| `--api-key <key>` | pass the API key non-interactively (skips the prompt) |
