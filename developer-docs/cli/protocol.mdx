---
title: 'protocol'
description: 'Non-custodial direct-to-chain operations.'
---

Non-custodial direct-to-chain operations.

## Subcommands

Run `kash protocol --help` for the full subcommand list. Each command is also documented at `kash protocol <subcommand> --help`.
