---
title: 'docs'
description: 'Emit the full command tree as JSON (or summary in human mode).'
---

Emit the full command tree as JSON (or summary in human mode).

> Generated from `kash docs --json`. Run `kash docs --help` for full usage and examples.

## Usage

```bash
kash docs [options]
```
